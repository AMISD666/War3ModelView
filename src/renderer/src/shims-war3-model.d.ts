// Deprecated: this shim used to paper over missing/unstable typings for `war3-model`.
// The project now resolves `war3-model` types via `tsconfig.web.json` paths:
// `vendor/war3-model/dist/war3-model.d.ts`.
export {};
